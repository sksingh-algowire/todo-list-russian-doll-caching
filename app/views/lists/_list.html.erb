<% cache list do %>
  <div style="float: left; width: 40%">
    <h2><%= link_to list.name, list %></h2>

    <%= link_to "Edit", edit_list_path(list)%>

    <%#= render partial: "todos/form", locals: {list: list} %>

    <ul>
      <%= render partial: 'todos/todo', collection: list.todos, cached: true %>
    </ul>
  </div>
<% end %>
